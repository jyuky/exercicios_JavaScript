if ( guess == location){
    hits = hits + 1; 
} else if ( guess == location2){
    hits = hits + 1; 
} else if ( guess == location3){
    hits = hits + 1; 
}

if  ( guess == location || guess == location2 || guess == location3 ){

    hits = hits + 1; 
}